<h2>Proje Teklifleri</h2>

<% if @requests.present? %>  <%# @project_requests yerine @requests olmalı! %>
  <ul>
    <% @requests.each do |request| %>
      <div>
        <h3><%= request.project.title %></h3>
        <p>Grup: <%= request.group.name %></p>
        <p>Lider: <%= request.group.leader.email %></p>

        <%= button_to "Kabul Et", accept_advisor_project_path(request.project), method: :post, class: "btn btn-success" %>
        <%= button_to "Reddet", reject_advisor_project_path(request.project), method: :post, class: "btn btn-danger" %>

      </div>
    <% end %>
  </ul>
<% else %>
  <p>Şu anda herhangi bir proje isteği bulunmamaktadır.</p>
<% end %>



